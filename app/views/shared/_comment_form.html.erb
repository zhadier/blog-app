<%= form_with url: user_post_comments_path(post_id:params[:id]), class: "comment-form", scope: :comment do |form| %>
  <%= form.text_area :text, class:"comment-form_text", autofocus: true, placeholder: "Add comment ...", required: true %>
  <%= form.submit "Comment" %>
<% end %>
